// Game over
// "Hello World" Equivalent

/*
C-style Block (Multi-line) commenting. 

'#' means preprocessor action (done before compiling)

'<iostream>' means "iostream file" and '<>' means "find it where compiler is located"

'int main()' means main() will return an integer value

'std::cout' means "from standard library, run 'cout'"

'<<' takes whatever is on the right and funnels it to whatever command is on the left

*/

#include <iostream>

int main()
{
    std::cout << "Game Over!" << std::endl;
    return 0; 
}